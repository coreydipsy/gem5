diff --git a/benchmarks/riscv/FFT b/benchmarks/riscv/FFT
new file mode 100755
index 0000000000..3758d3732d
Binary files /dev/null and b/benchmarks/riscv/FFT differ
diff --git a/benchmarks/riscv/LU b/benchmarks/riscv/LU
new file mode 100755
index 0000000000..ffccbb573e
Binary files /dev/null and b/benchmarks/riscv/LU differ
diff --git a/benchmarks/riscv/RADIX b/benchmarks/riscv/RADIX
new file mode 100755
index 0000000000..2c4cee83cc
Binary files /dev/null and b/benchmarks/riscv/RADIX differ
diff --git a/benchmarks/x86/FFT b/benchmarks/x86/FFT
new file mode 100755
index 0000000000..00fde21595
Binary files /dev/null and b/benchmarks/x86/FFT differ
diff --git a/benchmarks/x86/LU b/benchmarks/x86/LU
new file mode 100755
index 0000000000..48f024acec
Binary files /dev/null and b/benchmarks/x86/LU differ
diff --git a/benchmarks/x86/RADIX b/benchmarks/x86/RADIX
new file mode 100755
index 0000000000..83f459fc25
Binary files /dev/null and b/benchmarks/x86/RADIX differ
diff --git a/configs/common/CacheConfig.py b/configs/common/CacheConfig.py
index 61c6a304d7..0ed817786e 100644
--- a/configs/common/CacheConfig.py
+++ b/configs/common/CacheConfig.py
@@ -114,7 +114,23 @@ def config_cache(options, system):
     if options.l2cache and options.elastic_trace_en:
         fatal("When elastic trace is enabled, do not configure L2 caches.")
 
-    if options.l2cache:
+    if options.l2cache and options.l3cache:
+        # Provide a clock for the L2 and the L1-to-L2 bus here as they
+        # are not connected using addTwoLevelCacheHierarchy. Use the
+        # same clock as the CPUs.
+
+
+
+        system.l3 = L3Cache(clk_domain=system.cpu_clk_domain,
+                                   **_get_cache_opts('l3', options))
+        system.tollcbus = L2XBar(clk_domain = system.cpu_clk_domain)
+
+
+
+        system.l3.cpu_side = system.tollcbus.mem_side_ports
+        system.l3.mem_side = system.membus.cpu_side_ports
+
+    elif options.l2cache:
         # Provide a clock for the L2 and the L1-to-L2 bus here as they
         # are not connected using addTwoLevelCacheHierarchy. Use the
         # same clock as the CPUs.
@@ -159,8 +175,17 @@ def config_cache(options, system):
 
             # When connecting the caches, the clock is also inherited
             # from the CPU in question
-            system.cpu[i].addPrivateSplitL1Caches(icache, dcache,
-                                                  iwalkcache, dwalkcache)
+            # system.cpu[i].addPrivateSplitL1Caches(icache, dcache,
+            #                                       iwalkcache, dwalkcache)
+            if options.l2cache and options.l3cache:
+                per_core_l2cache = l2_cache_class(
+                                clk_domain=system.cpu_clk_domain,
+                                **_get_cache_opts('l2', options))
+                system.cpu[i].addTwoLevelCacheHierarchy(icache, dcache,
+                per_core_l2cache)
+            else:
+                system.cpu[i].addPrivateSplitL1Caches(icache, dcache,
+                iwalkcache, dwalkcache)
 
             if options.memchecker:
                 # The mem_side ports of the caches haven't been connected yet.
@@ -186,7 +211,11 @@ def config_cache(options, system):
                         ExternalCache("cpu%d.dcache" % i))
 
         system.cpu[i].createInterruptController()
-        if options.l2cache:
+        if options.l3cache:
+            system.cpu[i].connectAllPorts(
+                system.tollcbus.cpu_side_ports,
+                system.membus.cpu_side_ports, system.membus.mem_side_ports)
+        elif options.l2cache:
             system.cpu[i].connectAllPorts(
                 system.tol2bus.cpu_side_ports,
                 system.membus.cpu_side_ports, system.membus.mem_side_ports)
diff --git a/configs/common/Caches.py b/configs/common/Caches.py
index 1468b953c7..7732ae690f 100644
--- a/configs/common/Caches.py
+++ b/configs/common/Caches.py
@@ -71,6 +71,15 @@ class L2Cache(Cache):
     tgts_per_mshr = 12
     write_buffers = 8
 
+class L3Cache(Cache):
+    assoc: int = 16
+    tag_latency: int = 20
+    data_latency: int = 20
+    response_latency: int = 20
+    mshrs: int = 512
+    tgts_per_mshr: int = 20
+    write_buffers: int = 256
+
 class IOCache(Cache):
     assoc = 8
     tag_latency = 50
diff --git a/configs/common/Options.py b/configs/common/Options.py
index a63cc7b089..834ccfdff2 100644
--- a/configs/common/Options.py
+++ b/configs/common/Options.py
@@ -153,6 +153,7 @@ def addNoISAOptions(parser):
     parser.add_argument("--l2_assoc", type=int, default=8)
     parser.add_argument("--l3_assoc", type=int, default=16)
     parser.add_argument("--cacheline_size", type=int, default=64)
+    parser.add_argument("--l3cache", action="store_true")
 
     # Enable Ruby
     parser.add_argument("--ruby", action="store_true")
diff --git a/powerful_script_commands.txt b/powerful_script_commands.txt
new file mode 100644
index 0000000000..266db5ad43
--- /dev/null
+++ b/powerful_script_commands.txt
@@ -0,0 +1,138 @@
+############################
+############################
+######### POWERFUL #########
+############################
+############################
+
+###################
+####### x86 #######
+###################
+
+##### FFT #####
+
+build/X86/gem5.opt --outdir=m5out/powerful/x86/FFT configs/example/se.py \
+--cpu-type=X86O3CPU \
+--cpu-clock=4GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=32kB \
+--l1i_assoc=8 \
+--l1d_size=64kB \
+--l1d_assoc=8 \
+--l2cache \
+--l2_size=2MB \
+--l2_assoc=16 \
+--l3cache \
+--l3_size=8MB \
+--l3_assoc=16 \
+--num-cpus=2 \
+--cmd=benchmarks/x86/FFT --options="-p2 -m16"
+
+
+##### LU #####
+
+build/X86/gem5.opt --outdir=m5out/powerful/x86/LU configs/example/se.py \
+--cpu-type=X86O3CPU \
+--cpu-clock=4GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=32kB \
+--l1i_assoc=8 \
+--l1d_size=64kB \
+--l1d_assoc=8 \
+--l2cache \
+--l2_size=2MB \
+--l2_assoc=16 \
+--l3cache \
+--l3_size=8MB \
+--l3_assoc=16 \
+--num-cpus=2 \
+--cmd=benchmarks/x86/LU --options="-p2 -n512"
+
+
+##### RADIX #####
+
+build/X86/gem5.opt --outdir=m5out/powerful/x86/RADIX configs/example/se.py \
+--cpu-type=X86O3CPU \
+--cpu-clock=4GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=32kB \
+--l1i_assoc=8 \
+--l1d_size=64kB \
+--l1d_assoc=8 \
+--l2cache \
+--l2_size=2MB \
+--l2_assoc=16 \
+--l3cache \
+--l3_size=8MB \
+--l3_assoc=16 \
+--num-cpus=2 \
+--cmd=benchmarks/x86/RADIX --options="-p2 -n1048576"
+
+
+#####################
+####### RISCV #######
+#####################
+
+##### FFT #####
+
+build/RISCV/gem5.opt --outdir=m5out/powerful/RISC-V/FFT configs/example/se.py \
+--cpu-type=RiscvO3CPU \
+--cpu-clock=4GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=32kB \
+--l1i_assoc=8 \
+--l1d_size=64kB \
+--l1d_assoc=8 \
+--l2cache \
+--l2_size=2MB \
+--l2_assoc=16 \
+--l3cache \
+--l3_size=8MB \
+--l3_assoc=16 \
+--num-cpus=2 \
+--cmd=benchmarks/riscv/FFT --options="-p2 -m16"
+
+
+##### LU #####
+
+build/RISCV/gem5.opt --outdir=m5out/powerful/RISC-V/LU configs/example/se.py \
+--cpu-type=RiscvO3CPU \
+--cpu-clock=4GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=32kB \
+--l1i_assoc=8 \
+--l1d_size=64kB \
+--l1d_assoc=8 \
+--l2cache \
+--l2_size=2MB \
+--l2_assoc=16 \
+--l3cache \
+--l3_size=8MB \
+--l3_assoc=16 \
+--num-cpus=2 \
+--cmd=benchmarks/riscv/LU --options="-p2 -n512"
+
+
+##### RADIX #####
+
+build/RISCV/gem5.opt --outdir=m5out/powerful/RISC-V/RADIX configs/example/se.py \
+--cpu-type=RiscvO3CPU \
+--cpu-clock=4GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=32kB \
+--l1i_assoc=8 \
+--l1d_size=64kB \
+--l1d_assoc=8 \
+--l2cache \
+--l2_size=2MB \
+--l2_assoc=16 \
+--l3cache \
+--l3_size=8MB \
+--l3_assoc=16 \
+--num-cpus=2 \
+--cmd=benchmarks/riscv/RADIX --options="-p2 -n1048576"
\ No newline at end of file
diff --git a/src/arch/x86/MinorCPU.py b/src/arch/x86/MinorCPU.py
new file mode 100644
index 0000000000..5b9ef97bb3
--- /dev/null
+++ b/src/arch/x86/MinorCPU.py
@@ -0,0 +1,2 @@
+from m5.objects.X86CPU import X86MinorCPU
+MinorCPU = X86MinorCPU
diff --git a/src/arch/x86/SConscript b/src/arch/x86/SConscript
index 9eeefd76f4..d2fa56a60f 100644
--- a/src/arch/x86/SConscript
+++ b/src/arch/x86/SConscript
@@ -78,6 +78,7 @@ SimObject('AtomicSimpleCPU.py', sim_objects=[], tags='x86 isa')
 SimObject('TimingSimpleCPU.py', sim_objects=[], tags='x86 isa')
 SimObject('NonCachingSimpleCPU.py', sim_objects=[], tags='x86 isa')
 SimObject('O3CPU.py', sim_objects=[], tags='x86 isa')
+SimObject('MinorCPU.py', sim_objects=[], tags='x86 isa')
 
 DebugFlag('LocalApic', "Local APIC debugging", tags='x86 isa')
 DebugFlag('X86', "Generic X86 ISA debugging", tags='x86 isa')
diff --git a/src/arch/x86/X86CPU.py b/src/arch/x86/X86CPU.py
index 0b46c94c6e..689860c723 100644
--- a/src/arch/x86/X86CPU.py
+++ b/src/arch/x86/X86CPU.py
@@ -33,6 +33,7 @@ from m5.objects.X86Decoder import X86Decoder
 from m5.objects.X86MMU import X86MMU
 from m5.objects.X86LocalApic import X86LocalApic
 from m5.objects.X86ISA import X86ISA
+from m5.objects.BaseMinorCPU import BaseMinorCPU
 
 class X86CPU:
     ArchDecoder = X86Decoder
@@ -60,3 +61,6 @@ class X86O3CPU(BaseO3CPU, X86CPU):
     # (it's a side effect of int reg renaming), so they should
     # never be the bottleneck here.
     numPhysCCRegs = Self.numPhysIntRegs * 5
+
+class X86MinorCPU(BaseMinorCPU, X86CPU):
+    mmu = X86MMU()
diff --git a/weak_script_commands.txt b/weak_script_commands.txt
new file mode 100644
index 0000000000..1ca106886a
--- /dev/null
+++ b/weak_script_commands.txt
@@ -0,0 +1,102 @@
+########################
+########################
+######### WEAK #########
+########################
+########################
+
+###################
+####### x86 #######
+###################
+
+##### FFT #####
+
+build/X86/gem5.opt --outdir=m5out/weak/x86/FFT configs/example/se.py \
+--cpu-type=X86MinorCPU \
+--cpu-clock=1GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=8kB \
+--l1i_assoc=4 \
+--l1d_size=16kB \
+--l1d_assoc=4 \
+--num-cpus=1 \
+--cmd=benchmarks/x86/FFT --options="-p1 -m16"
+
+
+##### LU #####
+
+build/X86/gem5.opt --outdir=m5out/weak/x86/LU configs/example/se.py \
+--cpu-type=X86MinorCPU \
+--cpu-clock=1GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=8kB \
+--l1i_assoc=4 \
+--l1d_size=16kB \
+--l1d_assoc=4 \
+--num-cpus=1 \
+--cmd=benchmarks/x86/LU --options="-p1 -n512"
+
+
+##### RADIX #####
+
+build/X86/gem5.opt --outdir=m5out/weak/x86/RADIX configs/example/se.py \
+--cpu-type=X86MinorCPU \
+--cpu-clock=1GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=8kB \
+--l1i_assoc=4 \
+--l1d_size=16kB \
+--l1d_assoc=4 \
+--num-cpus=1 \
+--cmd=benchmarks/x86/RADIX --options="-p1 -n1048576"
+
+
+#####################
+####### RISCV #######
+#####################
+
+##### FFT #####
+
+build/RISCV/gem5.opt --outdir=m5out/weak/RISC-V/FFT configs/example/se.py \
+--cpu-type=RiscvMinorCPU \
+--cpu-clock=1GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=8kB \
+--l1i_assoc=4 \
+--l1d_size=16kB \
+--l1d_assoc=4 \
+--num-cpus=1 \
+--cmd=benchmarks/riscv/FFT --options="-p1 -m16"
+
+
+##### LU #####
+
+build/RISCV/gem5.opt --outdir=m5out/weak/RISC-V/LU configs/example/se.py \
+--cpu-type=RiscvMinorCPU \
+--cpu-clock=1GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=8kB \
+--l1i_assoc=4 \
+--l1d_size=16kB \
+--l1d_assoc=4 \
+--num-cpus=1 \
+--cmd=benchmarks/riscv/LU --options="-p1 -n512"
+
+
+##### RADIX #####
+
+build/RISCV/gem5.opt --outdir=m5out/weak/RISC-V/RADIX configs/example/se.py \
+--cpu-type=RiscvMinorCPU \
+--cpu-clock=1GHz \
+--cacheline_size=64 \
+--caches \
+--l1i_size=8kB \
+--l1i_assoc=4 \
+--l1d_size=16kB \
+--l1d_assoc=4 \
+--num-cpus=1 \
+--cmd=benchmarks/riscv/RADIX --options="-p1 -n1048576"
\ No newline at end of file
